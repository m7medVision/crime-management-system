# Server configuration
SERVER_PORT=8080
SERVER_ENVIRONMENT=development
SERVER_READ_TIMEOUT=60
SERVER_WRITE_TIMEOUT=60

# Database configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=password
DB_NAME=crime_management
DB_SSLMODE=disable

# Authentication
AUTH_SECRET=my-development-secret-key
AUTH_EXPIRY_TIME=24

# Email configuration
EMAIL_RESEND_API_KEY=your-development-resend-api-key
EMAIL_FROM=dev-no-reply@districtcore.gov
EMAIL_FROM_NAME=District Core Crime Management (DEV)

# Storage configuration
STORAGE_TYPE=local
STORAGE_LOCAL_PATH=./storage
